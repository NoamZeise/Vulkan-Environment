# The publicly accessable functions that can be used by the graphics backends,
# or by any projects using the backends.
cmake_minimum_required(VERSION 3.10)
project(graphics-api)
add_subdirectory(src)
